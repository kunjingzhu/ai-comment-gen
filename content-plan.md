# 第一篇内容脚本：《我用OpenClaw 1天做了个VSCode插件，能自动写注释》

## 标题备选
- 《10年前端，用AI 1天做了个VSCode插件，同事都在用》
- 《我让OpenClaw帮我写了个VSCode插件，效果惊人》
- 《手把手：用AI 1天开发一个VSCode插件（附完整代码）》

## 平台
- **掘金**（技术社区，前端聚集地）→ 完整技术文
- **知乎** → 问题导向："前端开发有哪些提高效率的神器？"
- **小红书** → 短视频/图文精简版
- **B站** → 录屏操作视频

---

## 正文（掘金/知乎版）

### 开头（抓注意力）

> 写注释这件事，做了10年前端了，从来没真正喜欢过。
>
> 不是不会写，是不想写——特别是接手别人的烂代码，每行都要猜"这到底在干嘛"。
>
> 上周试了一下让 AI 来写注释，结果一发不可收拾。**1天时间，我做了一个 VSCode 插件**，选中代码 → 右键 → AI 自动生成注释。
>
> 效果怎么样？看下面这段代码：

```javascript
// 改造前：看半天不知道在干嘛
const a = items.filter(x => x.status === 'active').sort((a, b) => b.priority - a.priority).slice(0, 5);

// 点击右键 → 「AI生成注释」后：
// 从活跃项目中按优先级排序，取前5条
const a = items.filter(x => x.status === 'active').sort((a, b) => b.priority - a.priority).slice(0, 5);
```

> 舒服了。

### 正文（为什么做）

> 10年前端，平时写 React、Node、各种工具脚本。遇到过太多不写注释的代码，也写过太多"以后补注释"然后永远没补的代码。
>
> AI 出来后我一直在想：能不能让它帮我们干这件事？
>
> 试了几个已有的方案：
> - 某些在线工具：还要复制粘贴，麻烦
> - ChatGPT：手动对话，更麻烦
> - 已有的 VSCode 插件：要么太慢，要么质量不行
>
> 所以干脆自己做一个。

### 正文（怎么做的）

> 技术栈非常简单：
>
> - **VSCode 插件**：Node.js，官方脚手架
> - **AI 接口**：DeepSeek API（速度够快，而且便宜，百万token只要几块钱）
>
> 核心流程就三步：

```javascript
// 1. 用户选中代码 → 右键点击
// 2. 调用 AI API 生成注释
// 3. 把注释插入到代码上方
```

> 整个插件主体代码只有 200 行。
>
> 最难的部分是什么？是写 Prompt。
>
> 要让 AI 生成"好的注释"，不能太啰嗦，也不能太简单。我试了十几种 Prompt 才找到平衡点：

```
你是一个资深前端工程师，请为以下代码生成简洁的中文注释。
只返回注释内容，不要多余的解释。
```

> 参数调了几个关键项：
> - **temperature: 0.3**（温度越低，输出越稳定）
> - **max_tokens: 1024**（防止生成太多废话）
> - **语言按 VSCode 配置自适应**（中文/英文都能无缝切换）

### 正文（效果实测）

> 我拿公司项目测了一下：

| 代码类型 | 生成速度 | 注释质量 |
|---------|---------|---------|
| 简单函数 | < 1 秒 | ⭐⭐⭐⭐⭐ |
| React 组件 | 1-2 秒 | ⭐⭐⭐⭐ |
| 复杂逻辑 | 2-3 秒 | ⭐⭐⭐⭐ |
| CSS/样式 | < 1 秒 | ⭐⭐⭐⭐⭐ |

> 对于 90% 的场景，生成的注释直接可用，改几个字就行。
> 剩下的 10%，主要是特别长的函数或业务逻辑太特殊的代码，需要人工微调。

### 结尾（引流 + 互动）

> 插件已开源，GitHub: https://github.com/kunjingzhu/ai-comment-plugin
> 也可以在 VSCode 插件商店搜索「AI 注释生成器」安装。
>
> 目前还是 MVP 版本，后续会加的功能：
> - 更多语言支持（Python、Go、Rust）
> - 自定义注释风格模板
> - 批量注释
>
> 你觉得 AI 写注释靠谱吗？欢迎评论交流 👇

---

## 小红书精简版

**标题：** 10年前端，用AI 1天做了个VSCode插件🔥自动写注释太香了

**正文：**

写代码不想写注释？😂看过来👇

我做了个VSCode插件：
选中代码 → 右键 → 「AI生成注释」
3秒搞定，不用自己写一个字

已经用DeepSeek API调通了
JS/TS/React都支持
简洁注释和详细JSDoc都可以选

🔗 开源免费，搜「AI 注释生成器」

#AI编程 #前端开发 #效率工具 #VSCode #程序员

---

## B站视频脚本提纲

1. **开场**（30秒）：展示痛点——没有注释的代码 vs 有注释的代码
2. **插件演示**（1分钟）：选中代码→右键→生成注释，全流程录屏
3. **技术拆解**（1分钟）：简单讲怎么做的，不深入，留到下一篇
4. **效果对比**（30秒）：生成前 vs 生成后
5. **结尾**（30秒）：引导安装、点赞关注
